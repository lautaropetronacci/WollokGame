import Dontwhackthecapybara.*
import personajes.*
import movimientos.*
import wollok.game.*

test "al golpear un topo sube el score" {
	topo.aplastadoPorMartillo()
	assert.equals(50,puntuacion.puntos())
}